
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: etcdclusters.etcd.oneinfra.ereslibre.es
spec:
  group: etcd.oneinfra.ereslibre.es
  names:
    kind: EtcdCluster
    listKind: EtcdClusterList
    plural: etcdclusters
    singular: etcdcluster
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: EtcdCluster is the Schema for the etcdclusters API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: EtcdClusterSpec defines the desired state of EtcdCluster
          properties:
            ca:
              description: Certificate represents a Certificate
              properties:
                certificate:
                  description: Certificate is a PEM encoded certificate
                  type: string
                privateKey:
                  description: PrivateKey is a PEM encoded private key
                  type: string
              type: object
            certificateAuthorities:
              description: CertificateAuthorities represents a set of Certificate
                Authorities
              properties:
                etcdClient:
                  description: Certificate represents a Certificate
                  properties:
                    certificate:
                      description: Certificate is a PEM encoded certificate
                      type: string
                    privateKey:
                      description: PrivateKey is a PEM encoded private key
                      type: string
                  type: object
                etcdPeer:
                  description: Certificate represents a Certificate
                  properties:
                    certificate:
                      description: Certificate is a PEM encoded certificate
                      type: string
                    privateKey:
                      description: PrivateKey is a PEM encoded private key
                      type: string
                  type: object
              type: object
            etcdMembers:
              description: The number of etcd members this cluster will manage. One
                etcd replica if not provided.
              type: integer
            etcdVersion:
              type: string
          type: object
        status:
          description: EtcdClusterStatus defines the observed state of EtcdCluster
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
